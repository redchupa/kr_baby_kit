[pytest]
testpaths = tests
# pytest-homeassistant-custom-component's entrypoint imports `fcntl` (Unix only),
# so we disable auto-load and let tests/integration/conftest.py opt in explicitly
# on POSIX runners.
addopts = -p no:homeassistant
asyncio_mode = auto
